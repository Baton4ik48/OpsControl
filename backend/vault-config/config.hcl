# =========================================================
# STORAGE (где хранит данные Vault)
# =========================================================
storage "file" {
  path = "/vault/data"
}

# =========================================================
# LISTENER (как подключаться к Vault)
# =========================================================
listener "tcp" {
  address     = "0.0.0.0:8200"
  tls_disable = 1
}

# =========================================================
# API + CLUSTER
# =========================================================
api_addr = "http://0.0.0.0:8200"
cluster_addr = "http://0.0.0.0:8201"

# =========================================================
# LOGGING
# =========================================================
log_level = "info"

# =========================================================
# UI
# =========================================================
ui = true